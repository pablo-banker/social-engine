# Copie para .env e ajuste conforme o ambiente.

PORT=8080

# Backend Go
# USE_API=false  -> a app usa dados mockados (nenhuma chamada HTTP é feita)
# USE_API=true   -> login/register/feed passam a chamar a Go API em API_BASE_URL
USE_API=false
API_BASE_URL=http://localhost:8080
